[package]
name = "paredit-feature-lint-clojure-depth"
description = "Lint rules for Clojure depth: core.async go-block discipline, contains? on a non-associative collection, and reference-type operator crossover"
readme = "README.md"
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
paredit-core-syntax = { path = "../../core/syntax" }
paredit-core-lint-engine = { path = "../../core/lint-engine" }
paredit-core-cli = { path = "../../core/cli" }
clap.workspace = true
serde_json.workspace = true

# Mandatory: without it this package silently opts out of the workspace lint
# table, including `unsafe_code = "deny"`, with no error at all.
[lints]
workspace = true
