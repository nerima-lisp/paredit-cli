//! Adds `:accessor` to every `defclass` slot that has neither `:accessor`,
//! `:reader`, nor `:writer`.
//!
//! One slice, one directory; the layers are names, not directories.

pub mod cli;
pub mod domain;
pub mod usecase;
