//! Synthesizes a `defgeneric` form for a name that has `defmethod` forms but
//! no declaration, from the methods' congruent lambda list.
//!
//! One slice, one directory; the layers are names, not directories.

pub mod cli;
pub mod domain;
pub mod usecase;
