//! The shared CLI shape every conditional conversion command uses.
//!
//! No domain or use case of its own: it is the argument type and runner the
//! six conversions share, which is why they are one package.

pub mod cli;
