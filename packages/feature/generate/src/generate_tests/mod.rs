//! Synthesizes a `deftest` skeleton for every definition `inspect test-map`
//! reports as untested.
//!
//! One slice, one directory; the layers are names, not directories.

pub mod cli;
pub mod domain;
pub mod usecase;
